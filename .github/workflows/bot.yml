name: YouLikeHits Points Bot

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  earn-points:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install playwright python-dotenv
          playwright install chromium
          playwright install-deps chromium

      - name: Run bot (continuous)
        env:
          YLH_LOGIN_ID: ${{ secrets.YLH_LOGIN_ID }}
          YLH_PASSWORD: ${{ secrets.YLH_PASSWORD }}
          GOOGLE_EMAIL: ${{ secrets.GOOGLE_EMAIL }}
          GOOGLE_PASSWORD: ${{ secrets.GOOGLE_PASSWORD }}
          YLH_USERNAME: ${{ secrets.YLH_USERNAME }}
          GOOGLE_COOKIES: ${{ secrets.GOOGLE_COOKIES }}
        run: python bot_cloud.py

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bot-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7